<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kalender Cuti Bersama</title>
  <link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.js"></script>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; background: #f9f9f9; }
    h2 { text-align: center; color: #333; }
    #calendar { max-width: 900px; margin: 20px auto; background: white; padding: 10px; border-radius: 12px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
  </style>
</head>
<body>
  <h2>📅 Kalender Cuti Bersama</h2>
  <div id="calendar"></div>

  <script>
  // ✅ Ganti URL ini dengan URL yang muncul di browser kamu (yang ada "script.googleusercontent.com/macros/echo?...")
  const sheetAPI = "https://script.google.com/macros/s/AKfycbwAIljawlyj2hspgR2LmOGubEqvkeWuorFtJSD5szTJXFyupE0-LagC4JcHe7RGdlH5/exec"; 

  fetch(sheetAPI)
    .then(res => res.json())
    .then(data => {
      console.log("Data dari Google Sheet:", data); // cek di console GitHub Pages

      // mapping data JSON ke event kalender
      const events = data.map(row => ({
        title: `${row.nama} (${row.jenis})`,
        start: row.start,
        end: row.end,
        description: row.keterangan || ''
      }));

      const calendarEl = document.getElementById('calendar');
      const calendar = new FullCalendar.Calendar(calendarEl, {
        initialView: 'dayGridMonth',
        events: events,
        eventColor: '#3788d8',
        displayEventTime: false
      });
      calendar.render();
    })
    .catch(err => console.error("Gagal ambil data:", err));
</script>

</body>
</html>
